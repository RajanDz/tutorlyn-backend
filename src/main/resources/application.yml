server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tutorlyn
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
app:
    jwtSecret: ${JWT_SECRET:"23232sdsssfaa41rfssaf32423fsaf235"}
    jwtExpirationMs: ${JWT_EXPIRATION_MS:3000}
    authAccessTokenCookie: ${access_token_cookie:"Rajolica"}