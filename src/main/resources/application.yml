server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tutorlyn
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    show-sql: true
app:
  jwtSecret: ${JWT_SECRET}
  jwtExpirationMs: ${JWT_EXPIRATION_MS}
  authAccessTokenCookie: ${access_token_cookie}